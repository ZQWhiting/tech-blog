<ol>
    {{#each posts as |post|}}
    <li>
        <div>
            {{title}}
        </div>
        <div>
            {{created_at}}
        </div>
    </li>
    {{/each}}
</ol>

<button>+ New Post</button>