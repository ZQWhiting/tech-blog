<ol class="item-no-style">
    {{#each posts as |post|}}
    <li>
        <div>
            {{title}}
        </div>
        <div>
            {{created_at}}
        </div>
    </li>
    {{/each}}
</ol>

<div class="btn-new-post-wrapper">
    <button class="btn-new-post">+ New Post</button>
</div>